# 2026-05-19 Git 동기화 및 데이터 파일 추가 작업 태스크 리스트

- [x] 원격 저장소에 반영되지 않은 로컬 커밋 및 작업 디렉토리 상태 분석
- [x] 백업 파일들이 무시되도록 `.gitignore` 규칙 추가 (`*_backup.py`, `*_backup_*.py`)
- [x] 신규 핵심 데이터 파일 스테이징 (`tv_ref_model_info.csv`, `cushion_hex.msh`, `9.TESTING_thumbnail_ISTA Project 6-Amazon.com-SIOC 18-18.pdf`)
- [x] 신규 파일 및 `.gitignore` 수정본을 커밋 (`feat(data): add model reference info, cushion mesh, and ISTA project document`)
- [x] 로컬의 미푸시 커밋(기존 1개 + 신규 1개)을 원격 `master` 브랜치로 최종 푸시 완료

## 🌀 관성 텐서 고유치(Eigenvalues) 검증 및 자동 보정 기능 추가 작업
- [x] `WHToolsBox` 물리엔진 강체 분석 레이어인 `whtb_physics.py` 내에 3x3 대칭 관성 행렬 고유치 수치 해석 검사 함수 개발
- [x] 고유치 조건 불만족 시(0 이하의 음수 고유치 검출 시) 대각 성분을 최소한으로 자동 Compensation(보정) 해주는 `_ensure_positive_eigenvalues` 필터 구축
- [x] `analyze_and_balance_components` 분석 함수에서 자동 계산된 `ic` 및 기존 로드된 `ic`에 대해 사전 고유치 타당성 다각 검증 파이프라인 연동
- [x] Rich 리포팅 터미널 블록(`_print_physics_report`) 내에 삼각부등식 대신 고상한 물리 분석인 고유치($\lambda_1, \lambda_2, \lambda_3$) 및 MuJoCo 호환 타당성 판정 가이드 출력 기능 통합
- [x] `run_drop_simulation_cases_v6.py`에 극단적 곱관성 타겟 설정을 부여하여 고유치 오류 사전 검출 및 정상 시뮬레이터 구동 안정성 최종 검증 완료
- [x] **[물리 엔진 역산 평행축 정리 전격 탑재]:** 가상 보정 바디가 최종 무게중심(`c_final`)으로 합성될 때 거리 차이로 인해 추가되는 관성을 계산 단계에서 선차감해주는 역산 평행축 정리($I_{\delta} = I_{target} - I_{base\_at\_tcog} - I_{parallel\_axis}(m_{\delta}, d_{\delta})$)를 `whtb_physics.py` 코어에 전격 반영 완료하여, **실제 빌드된 최종 시뮬레이터 조립체 관성(Final)이 사용자가 지정한 Target 관성과 소수점 4자리까지 오차 0.0%로 완벽히 일치하도록 조치 완료!**

## 🖥️ UI 내 Inertia Correction 진단 및 물리 성립 가이드 시스템 구축
- [x] `whts_control_panel.py`의 `ComponentBalanceDialog` UI 내에 실시간 물리 성립 가능성과 보정 가이드를 노출하는 전용 RichText 라벨 위젯 (`self.lbl_guide`) 신설
- [x] 다이얼로그 계산 함수 `_calculate_delta_inertia` 실행 시, solver 엔진과 100% 동일한 삼각부등식 클램핑 및 고유치 자가 치유 알고리즘 필터를 동시 적용하도록 개편
- [x] **[UI 계산부 역산 평행축 정리 완벽 동기화]:** 다이얼로그 계산부에서도 코어 계층과 정확히 싱크되는 역산 평행축 정리를 구현하여, UI 상에 표기되는 Delta 보정 값과 실제 MuJoCo 시뮬레이션 모델 조립 후 측정되는 최종 관성값이 오차 없이 **100% 일치하도록 보장**
- [x] 보정 전 로우(raw) 텐서의 고유치 및 자동 보정이 적용된 최종 고유치($\lambda_1, \lambda_2, \lambda_3$)를 산정하는 분석 로직 구축
- [x] 결과 표(`table_results`) 구조를 5행으로 확장하여 "Eigenvalues" 전용 행을 구축하고, 통과 여부(`Valid`) 및 보정 조치 여부(`Auto-Comp`)를 테이블 상에 실시간 명시
- [x] 고유치 오류가 예상되는 극단적 관성 모멘트 입력 시, 가이드 패널에 보정 전후 고유치 대비와 시뮬레이션 크래시 예방을 위한 가이드를 즉각 노출하도록 피드백 UI 최종 연동
- [x] `Calculate` 버튼 클릭 시, 검증에 성공한 경우에는 사용자 조작 편의를 도모하기 위해 정보성 팝업을 띄우지 않고 묵시적으로 표와 가이드창만 업데이트하며, 물리 사양 한계를 초과하여 **보정이 수행된 실패(보정) 시나리오에만 경고 팝업을 띄우도록 최적화 완료**
- [x] 다이얼로그 UI 및 터미널 출력 상에 사용되던 기존 "고윳값" 명칭을 기계공학 표준 학술 용어인 **"고유치"**로 100% 일괄 교명 완료
- [x] UI 가이드 출력 텍스트 상에서 "완벽하게", "완벽히" 등 불필요한 주관적 수식어를 전면 제거하여 명료하고 정제된 공학 리포트 스타일 구현 완료

## 📦 Size and ISTA 6-Amazon 낙하 자세 명명법 맵핑 연동
- [x] `IstaSetupHelperDialog` 내에 기존 방향 좌표계 명칭(예: `front-bottom-left`)을 ISTA 표준 규격 면 번호 명명법(예: `Corner 3-4-6`)으로 자동 매핑해 주는 `_convert_to_ista_direction_name` 변환기 개발
- [x] LTL/Parcel 낙하 시퀀스 테이블에서 특정 행(Posture)을 더블클릭하거나 아래 `Apply Selected Drop Posture` 버튼 클릭 시, 해당 모드에 맞춰 `Face X`, `Edge X-Y`, `Corner X-Y-Z`의 규격 문자열로 자동 변환하여 `self.config["drop_direction"]`에 대입하도록 수정 완료
- [x] 이산화 해석 빌더 코어(`parse_drop_target` 맵핑 파서)와의 연동성 및 빌드 검증 통과 완료
- [x] `IstaSetupHelperDialog` 내 Spec 입력 영역(`input_layout`) 배치에서 `Select Ref. Model` 및 `Mode` 라디오 버튼 그룹 위젯군을 `Pkg W (m):` 보다 상위 행(최상단 Row 0)에 위치하도록 레이아웃 재배치 완료
- [x] `IstaSetupHelperDialog` 모달 오픈 시, 부모 창(`ModelSetupDialog`)의 정중앙에 기하학적으로 완벽히 정렬하고 Y축 좌표가 최소 한계값(45px) 이상이 되도록 자동 보정하여 모니터 상단을 뚫고 나가는 "제목 표시줄 숨겨짐" 현상을 완벽히 해결 완료
- [x] **[ISTA UI 완전 플랫화]:** 이중 박스 테두리로 화면 낭비와 가독성 저하를 불러오던 `self.main_standard_group = QGroupBox("ISTA 6-Amazon Standard")` 마저 완전히 소멸시키고, 대신 내부 정보들을 메인 수직 레이아웃(`ista_lay`)에 여백과 함께 다이렉트로 귀속시켜 테두리 없이 시원하고 평탄한 프리미엄 명세서 스타일로 모던 레이아웃 개편 완료!
- [x] Control Center 메인 UI (`ControlPanel`)의 `Simulation Status` 내에 있던 `Step:` 라벨과 `Snapshots:` 라벨을 동일한 QHBoxLayout 수평 라인 상에 좌정렬하여 병렬 배치하고, Target 설정 QDoubleSpinBox(`spin_duration`)가 슬래시(`/`) 라벨 바로 옆에 틈새 없이 다닥다닥 밀착하여 출력되도록 우측 여백 Stretch 밀어내기 정렬 적용 완료

## 🔇 실시간 입력 조작 시 터미널 물리 분석 테이블 출력 조건 제어
- [x] `IstaSetupHelperDialog` 내에서 custom drop height 및 패키지 치수 변경에 따른 실시간 질량 계산 시, 불필요하게 콘솔을 채우던 관성 분석 보고서(`📊 [WHTOOLS] Physics Summary...`) 출력을 완전 차단 조치 완료
- [x] `whtb_builder.py` 내의 `create_model` 호출 시 `logger` 매개변수가 print가 아닌 무음 함수로 넘어올 경우 `is_verbose = (logger == print)` 분기를 통해 `verbose=False`로 전격 작동하도록 제어부 갱신 완료
- [x] `whtb_physics.py` 내의 `_clamp_inertia_triangle` 및 `_ensure_positive_eigenvalues` 내부의 경고 print 출력들 또한 `verbose` 플래그의 영향을 받아 출력되지 않도록 설계 보강 완료하여, 순수 갱신 작업 시 완전한 침묵(Silent) 달성 완료
