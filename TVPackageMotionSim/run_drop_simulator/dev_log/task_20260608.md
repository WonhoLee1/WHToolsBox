# OpenRadioss 프레임 매핑 수정 작업 태스크 리스트 (2026-06-08)

- [x] `whts_control_panel.py` 백업 생성 (`whts_control_panel.py.bak`)
- [x] `whts_radioss_builder.py` 백업 생성 (`whts_radioss_builder.py.bak`)
- [x] `whts_control_panel.py` 내부 중복 `_on_create_radioss_model` 메서드 통합
- [x] `_on_create_radioss_model`에서 현재 슬라이더 프레임 인덱스(`self.slider.value()`)를 `export_radioss`에 전달하도록 연동
- [x] `whts_radioss_builder.py`에서 초기 선속도 벡터(`self.v_vec * 1000.0`) 및 각속도 벡터(`self.omega_vec`)를 `/INIVEL/` 카드로 매핑 기입하도록 수정
- [x] 속도가 거의 0에 수렴하는 극초기 상태일 때 기존의 중력 자유 낙하 기준 속도 `-v0`를 Z축 속도로 사용하도록 폴백 로직 구현
- [x] 수정된 스크립트 파일들에 대한 Python 문법 컴파일 오류 여부 사전 검증 완료
- [x] OpenRadioss 초기 속도 카드 ID 중복 에러(`/INIVEL/ROT/1` -> `/INIVEL/ROT/2`) 디버깅 및 수정 완료
- [x] OpenRadioss 스텝 출력 빈도 제어 매개변수화 기능 추가 (기본값: `-10` 사이클 주기로 변경 완료)
- [x] 외부 툴(ParaView, LS-PrePost) 실행 시 Windows 백슬래시 인코딩 및 원화 기호(₩) 경로 인식 오류 수정 완료 (`.as_posix()` 적용)
- [x] HDF5 .vtkhdf 결과 파일 작성 시 Windows 파일 잠금 해제 설정 추가 (`HDF5_USE_FILE_LOCKING = FALSE` 적용)
- [x] 기존 결과 파일이 파라뷰 등에 의해 잠겨 지워지지 않을 때, 순차적 대체 파일명(`_1`, `_2` 등)을 부여하여 해석/로드가 중단되지 않고 기동되도록 개선 완료
- [x] 0바이트 vtkhdf 잔존 파일 자동 감지 및 컨버터 내부 윈도우 백슬래시 경로 전달 오류 수정 완료 (st_size < 100바이트 강제 갱신 및 .as_posix() 적용)
- [x] Box 쉘 메쉬 치수를 중면(Mid-surface) 기준으로 오프셋 변경 완료 (`bw + bt`, `bh + bt`, `bd + bt` 적용으로 Cushion과의 초기 침투 방지)
- [x] **[추가] 터미널 진행 상황 테이블 구분선(━) 길이를 헤더와 본문에 맞게 128자로 연장 완료**
- [x] **[추가] OpenRadioss 시뮬레이션 지속 시간(t_end) 및 엔진 로그 출력 간격의 GUI 동기화 기능 개선 완료**
  - `whts_radioss_builder.py`에서 `export_radioss_time`을 1순위로 조회하고, 누락 혹은 기본값(0.05초)일 때 `radioss_sim_duration` 및 `sim_duration`으로 폴백 연동하도록 개선하여 GUI 'Target Time (s)' 수치 조정이 엔진 지속 시간(t_end)으로 정확히 넘어가도록 조치 완료.
  - `whtb_config.py`의 기본값 딕셔너리에 `radioss_sim_duration`, `radioss_print_interval`, `export_radioss_time`, `export_radioss_dt_anim`을 모두 추가하여 설정 로딩 오동작을 미연에 차단.
  - `whts_control_panel.py`의 `CONFIG_METADATA`에 위 네 가지 설정을 모두 등록하여 GUI 'Solver' 탭 하위 및 각 스핀박스(`spin_rad_time`, `spin_rad_dt`) 조작 시 설정 사전이 실시간으로 연계되도록 개선 완료.
- [x] **[추가] OpenRadioss 모델 지면 오프셋 공중 부양 오류 디버깅 및 수정 완료**
  - MuJoCo와 OpenRadioss 간의 파트 초기 기하학적 Z축 기준 중심(Z-center) 불일치 현상 규명.
  - `whts_radioss_builder.py`에서 기존에 `cz=bd/2` (또는 로컬 적층 구조)로 메싱을 높여 만들던 Z축 레이아웃을, MuJoCo의 Pivot 계산 기준에 맞춰 `cz=0.0`(Assy 중심)으로 완벽히 정렬 동기화하여 지면에 딱 밀착되도록 개선 완료.
- [x] 수정 사항에 대한 개발로그 및 작업 내역 문서화 완료
