# Task List

- [x] `whts_engine.py`에 스레드별 MuJoCo 제어 콜백 디스패치 로직(`_mujoco_thread_registry` 및 `_global_mujoco_control_callback`) 추가
- [x] `whts_engine.py`의 `DropSimulator.setup()` 로직에서 전역 디스패처에 스레드 ID로 콜백을 등록하도록 수정 및 예외 처리 구문(`set_mjcb_control(None)`) 수정
- [x] `whts_control_panel.py`의 `BatchRdsWorker.run_one()` 종료 로직에서 `set_mjcb_control(None)` 대신 전역 레지스트리에서 스레드를 `pop`하도록 수정
- [x] 백업 파일 저장 (`dev_log`)
