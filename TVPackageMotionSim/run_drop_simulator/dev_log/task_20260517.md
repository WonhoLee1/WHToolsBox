# Model Configuration & Setup UI 개선 작업 목록

- [x] [NEW] `whts_ista_helper.py` 모듈 구현 (독립적인 ISTA 6A 시뮬레이션 자세 및 시퀀스 연산)
- [x] [MODIFY] `box_motion.py` 수정 (`REFERENCE_MODELS` 동적 CSV 로더 적용 및 fallback 지원)
- [x] [MODIFY] `whts_control_panel.py` 수정:
  - [x] `tv_ref_model_info.csv`를 읽는 PySide6 용 참조 모델 로더 함수 구현
  - [x] [NEW] `SelectTVModelDialog` 구현 (QTableWidget 기반 정렬 가능한 TV 규격 검색 및 선택 다이얼로그)
  - [x] `ModelSetupDialog` 레이아웃 및 콤보 박스 수정:
    - [x] GENERAL 모드용 동적 드롭다운(Face / Edge / Corner) 컨테이너 및 연동 로직
    - [x] PARCEL / LTL 모드용 **Select Sequence** 버튼 및 read-only 텍스트 배치
  - [x] [NEW] `IstaSetupHelperDialog` 구현 (ISTA 6A 시퀀스 테이블, 규격 진단, Face Numbering 안내 라벨, 일괄 주입 동기화)
- [x] [MODIFY] `whts_control_panel.py` PySide6 네이티브 프리미엄 다크 스타일 통일:
  - [x] `ModelSetupDialog`, `IstaSetupHelperDialog`, `SelectTVModelDialog` 다이얼로그 전반에 걸쳐 일관성 있는 **Premium Dark Theme** 스타일시트 적용
  - [x] 상단 Package Spec & Shipment Method 그룹 패널의 가로 폭을 줄이고 compact하게 재조정
  - [x] 각 입력 상자(Width, Height, Depth, Mass)의 가로 크기를 줄여 정갈하고 조밀하게 렌더링 (`setFixedWidth(90)`)
  - [x] Depth/Thickness (mm) 라벨 텍스트를 "Depth (mm)"로 변경
  - [x] ISTA 6-Amazon Test Sequence Table에서 의미 없는 "Tilt Latitude (deg)" 및 "Detailed Description" 열 제거하여 4열로 최적화
  - [x] 테이블 데이터 동적 바인딩 로직을 4열 구조에 맞춰 수술적으로 수정
  - [x] 다이얼로그 전체 치수를 `800x700`으로 콤팩트하게 축소하여 가시성 극대화
- [x] [MODIFY] `whts_control_panel.py` 내 Presets & Drop Setup(`setup_group`)의 개별 위젯 높이를 `25px`로 일괄 제한하고 정렬 마진을 축소하여 슬림화 완료
- [x] [MODIFY] `whts_control_panel.py` 내 `CONFIG_METADATA` 데이터 구조를 70개 이상의 전체 설정 키 설명 및 대분류 구조로 전면 확장하여 빈 설명이 노출되는 문제 완벽 해결
- [x] [MODIFY] `whts_control_panel.py` 내 `VisualSchematicWidget` 수정: Box 텍스트는 사각형 외곽 상단에 배치하고, TV 라벨을 SET로 이름 변경 및 내부 배치 유지 완료
- [x] 통합 수동 검증 및 시각 모니터링 수행
- [x] `py_compile`을 통한 무오류 정적 구문 안정성 검증 완료
- [x] [MODIFY] `whts_control_panel.py` 내 Mass & Dynamic Reporting의 이름을 `Mass, CoG, MoI`로 변경 및 `⚖️ Balance` 버튼 추가
- [x] [NEW] `ComponentBalanceDialog` 구현 (QDoubleSpinBox 기반 목표 사양 입력, 가중치/우선순위 슬라이더 제어)
- [x] [NEW] SLSQP Scipy-based 다차원 최적화 연산 엔진 구현 (Box 한계 경계 구속조건 및 CoG vs MoI 가중치 목적 함수 통합)
- [x] [NEW] 5열로 구성된 고해상도 비교 결과 QTableWidget 적용 (Initial, Target, Balanced, Status)
- [x] [NEW] 질량추 수 자동 최적화 및 추천 기능 (`Suggest Optimal Count` 마법사 다이얼로그 연동)
- [x] [MODIFY] `whts_control_panel.py` 내 "Mass, CoG, MoI" 그룹 패널을 config key-value 트리 테이블보다 상단(top_splitter와 mid_splitter 사이)에 배치하여 시각적 직관성 극대화 완료
- [x] [MODIFY] QSpinBox / QDoubleSpinBox 스타일시트를 수정하여 기형적인 native 닷(.) 버튼 대신 현대적인 side-by-side 인라인 SVG chevron (^, v) 형태로 전면 교체 완료
- [x] [MODIFY] Playback Controls 그룹박스 내 버튼들의 height를 24로 조절하여 레이아웃 조밀성 개선
- [x] [MODIFY] Simulation Speed 설정을 Timeline Navigation 그룹박스 내부 슬라이더 바로 아래로 통합 및 빈 Simulation Speed 그룹박스 완전 삭제
- [x] [MODIFY] Interactive 그룹박스 내 모든 버튼들의 height를 25로 고정하여 통일성 및 슬림화 완료

## ⚖️ 밸런싱 결과 테이블 왼쪽 아래 배치 레이아웃 개편
- [x] [MODIFY] `whts_control_panel.py`:
  - [x] `ComponentBalanceDialog._init_ui` 내 레이아웃을 좌/우 2열 배치(`QHBoxLayout`)로 구성
  - [x] 좌측 열(`left_layout`): 상단에 `specs_group`, 하단에 `results_group` 수직 적층 (`QVBoxLayout`)
  - [x] 우측 열(`right_layout`): 상단에 `focus_group`, 하단에 수직 stretch 배치 (`QVBoxLayout`)
  - [x] 좌측 열에 stretch ratio 2, 우측 열에 stretch ratio 1 부여하여 좌측이 약 65~70% 너비를 넓게 쓰도록 조정
  - [x] 다이얼로그 최소 해상도를 `(950, 620)`으로 미세 보정하여 가로 테이블 텍스트 시야 확보
  - [x] 정적 구문 컴파일 및 단위 테스트 `test_balance_dialog.py` 실행 검증

## ⚖️ Mass, CoG, MoI 정보 출력 가독성 개선 (줄바꿈 및 (diag) 제거)
- [x] [MODIFY] `whts_control_panel.py`:
  - [x] `self.report_label` 플레이스홀더 텍스트를 "Total Mass: - kg | CoG: -\nMoI: -"로 분할 구성
  - [x] `_update_reporting` 내 live 정보 출력 시 `Total Mass`와 `CoG`는 첫 줄에, `MoI`는 다음 줄(`<br/>`)에 줄바꿈 출력
  - [x] `MOI (diag)` 명칭에서 `(diag)`를 완전히 제거하여 `MoI`로 간소화
  - [x] 정적 구문 컴파일 및 단위 테스트 성공 검증

## ⚖️ Add Custom Mass 버튼의 밸런싱 최적화 다이얼로그(Balance UI) 내부 통합 개편
- [x] [MODIFY] `whts_control_panel.py`:
  - [x] `ModelSetupDialog` 메인 레이아웃 및 `"Mass, CoG, MoI"` 패널에서 "➕ Add Custom Mass" 버튼 완전히 제거
  - [x] `ModelSetupDialog` 클래스 내의 안 쓰는 중복 메서드 `_on_add_mass` 완전 삭제 (코드 위생)
  - [x] `ComponentBalanceDialog` 하단 버튼 레이아웃(`btn_run_lay`) 중간에 "➕ Add Custom Mass" 버튼 추가 통합
  - [x] `on_add_custom_mass` 메서드 구현: QInputDialog 입력받아 `self.custom_masses`에 누적 및 `self.config`에 즉시 반영
  - [x] `run_optimization_engine`에서 `AutoBalance_` 질량추만 소거하여, 커스텀 부가 질량을 기본 섀시 질량에 자동 편입(솔버 반영)
  - [x] `on_apply_clicked`에서 `self.custom_masses`와 `self.optimized_masses`를 이중 루프로 병합 저장 동기화 완료
  - [x] 정적 구문 컴파일 및 오프라인 테스트 성공 검증

## 📏 Package Spec 모든 입력 단위 미터(m) 일원화 및 실수 입력(QDoubleSpinBox) 적용
- [x] [MODIFY] `whts_control_panel.py` (`IstaSetupHelperDialog`):
  - [x] `Width`, `Height`, `Depth` 라벨의 단위를 `(mm)`에서 `(m)`로 일괄 변경
  - [x] 세 스핀박스(`spin_w`, `spin_h`, `spin_d`)의 범위를 `0.01 ~ 5.0` (m)로 조정하고, 실수값 정밀도를 소수점 3자리(`setDecimals(3)`)로 확장
  - [x] 스핀박스 초기 설정 시 `self.config`에 미터법으로 저장된 `box_w`, `box_h`, `box_d`를 `* 1000.0` 배율 곱셈 없이 직접 바인딩
  - [x] `_on_select_ref_model`: DB에서 가져온 `pkg_size` (mm 단위) 파싱 결과를 `w_mm / 1000.0` 미터법으로 자동 스케일링 변환하여 스핀박스에 대입
  - [x] `_update_all`: ISTA6ASimulator 호출 시 입력 미터 값에 `* 1000.0`을 곱해 내부 밀리미터 연산 규격(mm)에 완벽 부합
  - [x] `_on_apply_and_sync`: 메인 패널에 저장 및 적용 시 기존 나누기 `1000.0` 없이 미터 값을 직접 `self.config`에 대입
  - [x] 정적 컴파일 체크 및 통합 오프라인 연산 테스트 통과 검증 완료

## 🛠️ ModelSetupDialog 메인 모델 설정 UI 정밀 수정 및 중복/레거시 제거 (Surgical Clean-up)
- [x] [MODIFY] `whts_control_panel.py` (`ModelSetupDialog`):
  - [x] `_init_ui`에서 중복 노출되던 `"Mass, CoG, MoI"` 그룹박스(`bottom_group`) 관련 레이아웃 코드를 완전히 제거
  - [x] `self.btn_select_ref_model_direct` ("🔍 Setup") 버튼을 클릭했을 때 직접 모델 DB 다이얼로그를 여는 대신 `self._on_select_sequence`가 호출되어 `ISTA 6-Amazon Test Setup Helper` 창이 열리도록 슬롯 연결 변경
  - [x] 더 이상 사용되지 않는 불필요한 메소드들(`_on_select_ref_model_direct`, `_update_reporting`, `_on_balance_clicked`)을 파일 내에서 완전히 영구적으로 제거 (Surgical Clean-up 완료)
  - [x] `__init__`, `_on_apply_value`, `_on_preset_changed`, `_on_general_dropdowns_changed`, `_on_numeric_ui_changed`, `_on_ista_changed` 내부에서 더 이상 호출할 대상이 없는 `self._update_reporting()` 구문들을 안전하고 완벽하게 제거
  - [x] 터미널에서 `py_compile` 컴파일 성공 검증 완료 (Exit Code: 0)

## 📦 ISTA 6-Amazon Test Setup Helper 다이얼로그 정밀 튜닝
- [x] [MODIFY] `whts_control_panel.py` (`IstaSetupHelperDialog`):
  - [x] 다이얼로그 전체 크기를 가로 540px, 세로 720px로 고정 (`setFixedSize(540, 720)`)
  - [x] Width, Height, Depth 스핀박스의 fixed width를 `90`에서 `125`로 상향 조정
  - [x] `input_layout` 및 그리드 컬럼 배치를 540px 가로 폭에 최적화하여 촘촘히 배열
  - [x] 하단 정보 표시부에 `QStackedWidget` 도입:
    - [x] `Page 0` (LTL/Parcel): 진단결과(`diag_group`), 면 가이드(`face_desc_box`), 시퀀스 테이블(`seq_group`) 통합 적층
    - [x] `Page 1` (Custom): **[Custom Drop Mode Guide]** 안내박스 및 2D 스키매틱 뷰인 `VisualSchematicWidget` 삽입
  - [x] `_update_all` 메서드에서 모드 변경 시 StackedWidget 인덱스 변경(0 or 1) 로직 추가 및 기존 가시성 변경 로직 삭제
- [x] 통합 수동 및 자동 검증 (`py_compile` 컴파일 검증 및 단위 기능 테스트)
- [x] `./dev_log/walkthrough_20260517.md`에 수행된 내용 반영 기록

## 🎛️ Custom 모드 대화형 Drop Direction 콤보박스 개편
- [x] [MODIFY] `whts_control_panel.py` (`IstaSetupHelperDialog`):
  - [x] Custom 모드 Drop Direction 입력 콤보박스 3개(`combo_custom_p1/p2/p3`)를 단 2개의 대화형 콤보박스(`combo_custom_type`, `combo_custom_direction`)로 리팩토링
  - [x] `combo_custom_type` 선택 값(`Face`, `Edge`, `Corner`)에 맞춰 세부 방향 콤보박스 `combo_custom_direction` 아이템 목록 동적 주입 및 신호 제어 구현
  - [x] `_load_config_values` 복원 시 섀시 규격/시그널 오버라이트를 차단하는 라디오 버튼 시그널 임시 차단 및 백필 복원 로직 설계 완료
  - [x] `_update_reporting` 및 `_on_apply_and_sync`에서 2개 콤보박스 최종 수렴 문자열을 config에 다이렉트 바인딩 완료
- [x] 고도화 단위 테스트 `test_ista_helper_ui.py`를 활용한 100% 검증 통과 (Exit Code: 0)

## 📴 Custom 모드 Package 2D Schematic Preview 제외
- [x] [MODIFY] `whts_control_panel.py` (`IstaSetupHelperDialog`):
  - [x] Custom 모드 전용 Page 1에서 `custom_schematic_group` 및 `VisualSchematicWidget` 생성/배치 코드 완전히 소거
  - [x] Custom 가이드 라벨 (`lbl_custom_guide`) 내 2D 스키매틱 관련 문구를 현대화된 Drop Type/Direction 안내 문구로 깔끔하게 수정
  - [x] `_on_custom_dropdown_changed` 및 `_update_all` 내부에서 `self.custom_schematic`을 참조 및 갱신하던 불필요 레거시 라인들 완벽히 삭제 (AttributeError 차단 완료)
- [x] 정적 컴파일 및 단위 테스트 100% 재검증 통과 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 최종 작업 성과 기록




## 🗃️ Model Setup Dialog Config Tree Components & Balance Category Integration
- [x] [MODIFY] `whts_control_panel.py` (`CONFIG_METADATA`):
  - [x] 기존 레거시 `Mass` 카테고리 9개 항목들(`mass_chassis`, `mass_oc`, `mass_cushion`, `mass_paper`, `mass_occ`, `target_mass`, `enable_target_balancing`, `num_balancing_masses`, `chassis_aux_masses`)을 CONFIG_METADATA에서 완전히 소거
  - [x] 다차원 중첩 딕셔너리 구조를 갖는 `"components"` 및 `"components_balance"` 키를 CONFIG_METADATA에 신설 등록
  - [x] 두 신규 카테고리가 트리 뷰어 생성 시 `Geometry` 바로 다음에 정렬될 수 있도록 사전 정의 순서 정밀 조정 완료
- [x] [VERIFY] `py_compile` 컴파일 성공 및 구문 오류 없음을 최종 확인 (Exit Code: 0)
- [x] [VERIFY] 전용 GUI 단위 테스트 스크립트 `test_components_metadata.py`를 실행하여 Mass 소거 여부, Geometry 바로 밑 안착 여부, 자식 노드의 재귀 렌더링 무결성을 100% 검증 통과 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그 최종 갱신 기록

## 🧲 Model Setup Dialog Config Tree Weld Physics Integration
- [x] [MODIFY] `whts_control_panel.py` (`CONFIG_METADATA`):
  - [x] `Weld Physics Constants` 관련 21개 레거시 단일 변수들(`cush_weld_solref_timec` ~ `paper_weld_solimp`)을 CONFIG_METADATA에서 완전히 소거
  - [x] 다차원 중첩 딕셔너리 구조를 갖는 `"welds"` 키를 CONFIG_METADATA에 신규 등록하여 통합 관리
- [x] [VERIFY] `py_compile` 정적 컴파일 무결점 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`를 확장 기동하여 레거시 단일 변수 전면 소거 여부와 `welds` 하위 자식 노드의 재귀 로딩 구조 무결성을 100% 검증 통과 완료 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 본 수술 성과 누적 기록 완료

## 🎛️ Component Assembly Mass, CoG & MoI Balancing Optimizer UI Simplification & Layout Redesign
- [x] [MODIFY] `whts_control_panel.py` (`ComponentBalanceDialog`):
  - [x] `box boundary (m)` 그룹 박스(`box_group`), 하위 스핀박스 3종, Select Ref. Model 버튼 및 `_on_select_box_ref_model` 메소드 전면 삭제 (Surgical Clean-up)
  - [x] 원래 box boundary가 위치하던 자리에 `Optimization Focus Priority` 슬라이더 튜닝 그룹 박스(`focus_group`)를 정확히 이식 배치
  - [x] 기존의 복잡했던 좌/우 2열 분할 레이아웃(`split_layout`)을 완전히 철거하고, 950px의 대형 결과 테이블의 가독성을 위해 **1열 수직 단일 레이아웃 적층 배치 (`QVBoxLayout`)**로 대격변 개편 단행
  - [x] SLSQP 최적화 엔진(`run_optimization_engine`) 내부에서 GUI의 box boundary 값을 읽는 대신, `self.config`에 들어있는 실제 패키지 치수(`box_w`, `box_h`, `box_d`)를 직접 동적 참조하여 최적화 제한 영역을 안전하게 계산하도록 이식
- [x] [VERIFY] `py_compile` 정적 무결점 빌드 컴파일 성공 확인 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`에 `ComponentBalanceDialog` 검증 케이스를 통합 확장하여 Box Boundary 위젯 완전 제거 여부, 최적화 SLSQP 엔진 수렴 여부를 모두 단언(assert)하고 100% 무오류 성공 통과 완료 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 본 성과 누적 기록 완료

## 📺 Model Configuration & Setup Dialog Presets & Drop Setup Simplification & Optimizer Integration
- [x] [MODIFY] `whts_control_panel.py` (`ModelSetupDialog`):
  - [x] `Presets & Drop Setup` 그룹 박스 내부의 수동 입력용 라벨, 콤보박스, 스핀박스 10여 개를 화면 레이아웃에서 전격 제외하여 극도의 간소화 실현
  - [x] 대신에 **`🔍 Setup`** 버튼과 신설된 **`⚖️ Mass, CoG, MoI`** 버튼 2종만 미려한 10pt 수직 적층 레이아웃(`QVBoxLayout`)으로 그룹 박스 내에 전면 재배치
  - [x] 타 클래스 연동 무결성 유지를 위해 기존 위젯 변수들(`self.combo_preset`, `self.combo_ista`, `self.edit_direction`, `self.spin_height`, `self.spin_azimuth`, `self.spin_lat` 등)을 객체 상에서 그대로 생성해 두고 레이아웃에서만 배제하는 **숨김 그림자 위젯(Hidden Shadow Widgets) 전략** 성공적 적용
  - [x] `ModelSetupDialog` 클래스 하위에 `_on_balance_clicked` 다이얼로그 기동 슬롯 메소드를 이식하여 `⚖️ Mass, CoG, MoI` 버튼 클릭 시 `ComponentBalanceDialog`가 모달로 안전하게 구동되도록 연동 완료
- [x] [VERIFY] `py_compile` 정적 무결점 빌드 컴파일 성공 확인 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`에 `ModelSetupDialog` 신설 버튼 생성 여부 및 바인딩 상태 단언(assert) 케이스를 보완하여 100% 무오류 성공 통과 완료 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 본 성과 누적 기록 완료

## 🐛 Model Setup Dialog Hidden Shadow Widgets Separate Window Bug Fix
- [x] [MODIFY] `whts_control_panel.py` (`ModelSetupDialog`):
  - [x] `self.combo_preset`, `self.combo_ista`, `self.direction_container_widget`, `self.edit_direction` 등 13종의 숨김 그림자 위젯(Hidden Shadow Widgets) 생성 시 부모 다이얼로그 객체 참조인 `self`를 명시적 인자로 주입하여 자식 객체 귀속 완료
- [x] [VERIFY] `py_compile` 정적 무결점 컴파일 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`를 실행하여 셋업 다이얼로그 기동 시 불필요한 별도 최상위 위젯 창이 떠오르는 부작용이 완전히 100% 소멸되었음을 확인하고 최종 통과 완료 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 본 해결 성과 누적 기록 완료

## ⚖️ Model Setup Dialog _update_reporting AttributeError Bug Fix
- [x] [MODIFY] `whts_control_panel.py` (`ModelSetupDialog`):
  - [x] `ModelSetupDialog` 클래스 하위에 `_update_reporting(self)` 멤버 메소드를 신설 설계하여 트리 및 시각 스키매틱을 순차 리프레시하도록 구현 (방어 코드 곁들임)
- [x] [VERIFY] `py_compile` 정적 컴파일 성공 재검증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`에 `ModelSetupDialog._update_reporting` 존재 여부 및 성공 호출 검증 단언(assert) 추가 및 100% 무오류 성공 통과 완료 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 본 해결 성과 누적 기록 완료

## 🎨 2D Visual Schematic Preview Painting & Layout Improvement
- [x] [MODIFY] `whts_control_panel.py` (`VisualSchematicWidget`):
  - [x] 박스 형상 사각형(`box_rect`)의 중심 기준 가로/세로 중심선 점선(`QtCore.Qt.DashLine`) 드로잉 로직 이식
  - [x] 최적화 타겟 무게중심(`target_cog`) 좌표를 2D 픽셀 스케일 매핑 및 반경 4.5px 네온 레드 원형 마커(`drawEllipse`) 마킹 완료
  - [x] 마커 점 우측에 밀리미터 단위 실제 X, Y 오프셋 좌표값 라벨 렌더링 기능 구현 완료
- [x] [VERIFY] `py_compile` 정적 무결점 컴파일 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`를 실행하여 2D 스키매틱 위젯 내부의 페인팅 연산 중 일체의 충돌이나 에러가 유발되지 않고 정상 렌더링 완료됨을 확증 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 본 해결 성과 누적 기록 완료

## ⚖️ Component Balance Dialog Resizing & MoI Optimization Performance Revamp
- [x] [MODIFY] `whts_control_panel.py` (`ComponentBalanceDialog`):
  - [x] 다이얼로그 생성 시 `QtCore.Qt.WindowMinMaxButtonsHint` 및 `self.setSizeGripEnabled(True)` 로 창 크기 조절 전격 허용
  - [x] `table_results` 테이블의 `Expanding` SizePolicy 선언 및 컬럼 너비 균등 Stretch 분할 기능 적용
  - [x] 보조 질량 공간 배치 한도 안전 계수를 `0.9`배에서 **`0.96`배**로 대폭 상향하여 물리/기하학적 MoI 획득 용량 추가 확보
  - [x] 손실 함수(Loss function) 내 MoI 상대 오차에 epsilon `1e-3` 보정 및 MoI 오차곱 penalty 가중치를 기존 `1.0` 에서 **`2.5e5`배**로 대폭 튜닝하여 타겟 MoI(14.0) 완벽 매칭 보증
- [x] [VERIFY] `py_compile` 정적 무결점 컴파일 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`를 기동하여 8개 보조 질량이 양 꼭짓점 끝단 방향으로 최적 스프레드 배치되어 MoI Target을 정확히 일치시켜 통과 완료 (Exit Code: 0)

## 🎛️ Config Tree In-place Value Editing & Real-time Synchronization
- [x] [MODIFY] `whts_control_panel.py` (`ModelSetupDialog`):
  - [x] `_add_dict_items` 및 `_populate_config_tree` 내 Leaf 노드(데이터 실값)의 Value 열에 대해 `ItemIsEditable` 플래그 설정 완료
  - [x] `self.config_tree` 트리 위젯의 `itemChanged` 시그널을 신설 슬롯 `_on_tree_item_changed`에 연결
  - [x] `_on_tree_item_changed` 슬롯을 신설 구현하여 col=3(Value 열) 더블클릭 수정 완료 시 `key_path` 경로를 역추적, `ast.literal_eval` 및 type casting 하여 `self.config` 값 즉시 갱신
  - [x] 값 변경 적용 시 스키매틱(`self.schematic`) 및 상세 에디터(`self.py_editor`) 내용 100% 동시 동기화 기능 구현
  - [x] 무한 시그널 루프 방지를 위해 트리 새로고침 시 `blockSignals` 방어 처리 완료
  - [x] 자료형 불일치 등 사용자 입력 오류 시 상태창에 경고 표시 후 원래 유효한 값으로 자동 복구해 주는 롤백(Rollback) 로직 탑재
- [x] [VERIFY] `test_components_metadata.py` 단위 테스트에 가상 트리 아이템 Value 열 직접 수정 후 `box_w` 변수가 2.45m로 실시간 동기화 업데이트되는 assertions 추가 및 성공 합격 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 최종 해결 성과 누적 기록 완료

## 🔕 Scipy SLSQP Bounds-Clipping RuntimeWarning Suppression
- [x] [MODIFY] `whts_utils.py` (`calculate_required_aux_masses`):
  - [x] `minimize` 구동 영역에 `warnings.catch_warnings` 및 `warnings.filterwarnings("ignore")` 컨텍스트 필터 적용 완료
- [x] [MODIFY] `whts_control_panel.py` (`run_optimization_engine`):
  - [x] 동일하게 `minimize` 호출부에 bounds-clipping RuntimeWarning 일시 무시 필터 장착 완료
- [x] [VERIFY] `py_compile` 정적 무결점 컴파일 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py`를 재기동하여 어떠한 RuntimeWarning 노이즈 출력도 없이 100% 무오류 수렴 통과 완수 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 최종 성과 누적 업데이트 완료

## 📐 Mathematical Verification of MoI Optimization and Nonlinear Boundary Constraint Integration
- [x] [CHECK] 보조 질량들의 개별 질량 크기(Add Mass)와 배치 반경 위치(Position)가 유기적으로 동시 변화하는 최적화 구조 점검 완료
- [x] [MODIFY] `whts_control_panel.py` (`run_optimization_engine`):
  - [x] 8개 배치 꼭짓점이 박스 한도 밖으로 나가는 것을 수치적으로 엄격 차단하는 비선형 부등식 제약조건 `constraint_physical_bounds` 구현 및 `minimize` constraints에 전격 수혈 완료
- [x] [MODIFY] `whts_utils.py` (`calculate_required_aux_masses`):
  - [x] 물리 바운더리 마진을 `0.9`배에서 **`0.96`배**로 상향하여 한도 통일 완료
  - [x] 최적화 탐색 변수 $dx, dy, dz$의 bounds 상한값을 고정값 `1.0` 대신 동적 편심 이동량이 고려된 `dx_max, dy_max, dz_max` 상한선으로 실시간 제한하여 클리핑 오차 원천 박멸
- [x] [VERIFY] `py_compile` 정적 무결점 컴파일 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] `test_components_metadata.py` 단위 테스트를 재기동하여, 8개 배치 지점에 완벽히 균일한 **`3.0250 kg` 의 완벽 대칭 질량이 정확히 매칭**되고, 박스 범위 클리핑 모순이 0%인 최고 정밀도의 MoI Target 피팅 성공 검증 완료 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 최종 해결 성과 누적 기록 완료

## 🌳 Tree View Hierarchy Flattening for Components & Weld Physics
- [x] [MODIFY] `whts_control_panel.py` (`_populate_config_tree`):
  - [x] 대분류 카테고리가 'Components' 혹은 'Weld Physics'인 경우, 껍데기 딕셔너리 키 노드(`"components"`, `"welds"`) 생성을 생략하고 대분류 노드 직계 자식으로 직결되도록 렌더링 간소화 완료
  - [x] 계층 간소화 상태에서도 UserRole 데이터 패스(`key_path`)는 오리지널 딕셔너리 경로를 보존하도록 코딩하여, In-place 값 편집 및 상세 에디터 동기화가 Seamless하게 100% 정상 가동되도록 완벽 조치 완료
- [x] [MODIFY] `test_components_metadata.py` (Unit Test):
  - [x] 껍데기 노드가 소멸하고 `auxboxmass`, `chassis`, `cushion` 등의 welds 개별 파츠 7개가 대분류 하위에 다이렉트로 매핑됨을 검증하도록 assert 구문 정밀 개편 완료
- [x] [VERIFY] `py_compile` 정적 컴파일 무결점 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py` 재실행하여 100% 성공 통과 확인 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 최종 성과 누적 기록 완료

## 🎨 Tree View QLineEdit Row Height & Styled Padding Corrections
- [x] [MODIFY] `whts_theme.py` (`TREE_QSS`):
  - [x] 트리뷰 개별 행 `QTreeWidget::item` 에 `min-height: 24px; padding: 3px;` 을 수혈하여 평상시 가독성 및 줄 간격 쾌적함 극대화
  - [x] 인플레이스 값 편집을 담당하는 자식 위젯 `QTreeWidget QLineEdit` 의 스타일을 신설하여 배경색은 `{C_BG_BTN_HOV}`, 테두리는 액센트 칼라 `{C_SEL}`, 둥근 모서리 및 안쪽 여백 `padding: 1px 4px;` 을 적용하여 텍스트 잘림 현상 100% 완전 차단
- [x] [VERIFY] `py_compile` 정적 컴파일 무결점 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py` 재실행하여 100% 성공 통과 확인 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 최종 성과 누적 기록 완료

## 📐 Compact 3-Column Unified Tree Layout & Configuration Key Column Merging
- [x] [MODIFY] `whts_control_panel.py` (Tree Structure):
  - [x] 트리뷰 컬럼 구성을 4개에서 **3개 (`["Configuration Key", "Description", "Value"]`)** 로 축소 조정 완료
  - [x] 0번째 열 너비를 120px에서 **260px**로 획기적으로 확장하여 들여쓰기 텍스트 안전 영역 확보 완료
  - [x] 모든 키명 설정(`setText(0, str(k))`) 및 UserRole 귀속을 0번째 열(col=0)로 통합하여 **화살표와 유기적인 들여쓰기 계층 구조가 동시 활성화**되도록 리팩토링 완료
  - [x] `Value` 표시를 3번째 열에서 2번째 열(col=2)로 이전 통합 완료
  - [x] 트리 클릭(`_on_config_item_clicked`) 및 직접 편집 동기화 슬롯(`_on_tree_item_changed`)의 참조 컬럼 인덱스를 col=0 (UserRole) 및 col=2 (Value)로 완벽히 정렬 보정 완료
- [x] [MODIFY] `test_components_metadata.py` (Unit Test):
  - [x] 3열 슬림 레이아웃 규격(col=0 UserRole, col=2 Value)에 기인하도록 가상 트리 수정 및 Welds 서브파트 이름 검증 인덱스 전격 보정 완료
- [x] [VERIFY] `py_compile` 정적 컴파일 무결점 성공 재입증 (Exit Code: 0)
- [x] [VERIFY] 단위 테스트 스크립트 `test_components_metadata.py` 재실행하여 100% 성공 통과 확인 (Exit Code: 0)
- [x] `./dev_log/walkthrough_20260517.md` 및 `./dev_log/task_20260517.md` 작업 로그에 최종 성과 누적 기록 완료












