- `[x]` OpenRadioss GUI 폴더에서 스크립트 파일 복사 및 프로젝트 배치
    - `[x]` `runopenradioss.py` 복사 및 `run_drop_simulator` 하위에 배치
    - `[x]` `inp2rad.py` 복사 및 `run_drop_simulator` 하위에 배치
- `[x]` 복사된 스크립트 수정 및 최적화
    - `[x]` `runopenradioss.py` 내의 `tkinter` 및 기타 GUI 모듈 로드 예외 처리 강화
    - `[x]` 메인 스레드가 아닌 다른 스레드에서 시그널 처리 시 예외 방지 (ValueError 우회)
- `[x]` `whts_radioss_builder.py` 연동부 리팩토링
    - `[x]` `subprocess.Popen([_sys.executable, "-c", script], ...)` 부분을 제거하고 로컬 모듈 직접 임포트 및 구동으로 변경
    - `[x]` Radioss 실행 시 `debug=0` 적용 및 콜백에 `[Radioss]` 접두사 없이 원본 stdout을 그대로 전달하도록 리팩토링
- `[x]` `runopenradioss.py` stdout 출력 양식 개선
    - `[x]` `job_process`에서 앞공백 `"  "` 없이 원본 라인 출력하도록 수정
- `[x]` PyInstaller 빌드 테스트 및 최종 검증
    - `[x]` `drop_simulator_v6.spec` 파일의 `hiddenimports`에 모든 내부/외부 누락 모듈(whts_, whtb_, openpyxl, numba, lxml 등) 보강
    - `[x]` 빌드 스크립트(`build_drop_simulator_v6.ps1`) 실행 및 테스트 (빌드 성공 완료)
- `[x]` 새로 프로그램 실행 시 stale Lock 파일 정리 기능 추가
    - `[x]` `whts_utils.py` 내의 `WHToolsSessionLogger._acquire` 함수에서 좀비(stale) 락 및 대칭 로그를 선제적으로 일괄 삭제하도록 로직 보완
    - `[x]` candidates 후보군 외에 디렉토리 내의 모든 *.lock 파일에 대해 프로세스가 점유하지 않는 경우 일괄 제거하도록 확장 개선
- `[x]` 시뮬레이션 종료 시 결과 CSV 및 PNG 그래프 기본 자동 출력 적용
    - `[x]` `whts_engine.py` 내의 `_build_and_save_result` 함수에 `SimulationDataExporter` 자동 실행 연동 완료
- `[x]` UI 메뉴 연결 및 Radioss 로깅 양식 개선
    - `[x]` `whts_control_panel.py` 상단 뷰 메뉴에 탐색기 폴더 열기(`Open Result Folder in Explorer`) 메뉴 추가 및 연동 완료
    - `[x]` `RadiossEngineWorker`의 stdout 출력이 로거를 타지 않고 print()를 통해 깨끗하게 한 번만 윈도우 원본 형태로 찍히도록 로깅 연결 수정 완료
- `[x]` 콘솔창 넓이(120열) 및 기동 시 대형 ASCII Art 타이틀 지원
    - `[x]` `configure_terminal_font` 내에 콘솔 버퍼 및 창 가로 크기(120열) 설정 적용 완료
    - `[x]` `run_drop_simulation_cases_v6.py` 기동 시 화면 클리어 후 대형 `WHTOOLS` ASCII Art 인쇄 연동 완료
- `[/]` Git Push 미완료 파일 및 브랜치 확인
    - `[x]` 로컬 브랜치 `D0410` 및 untracked 작업 디렉토리 현황 파악 (Git 상태 조사 완료)
    - `[ ]` 작업 완료 후 커밋 & 푸시 진행
