# [v4.3] 컨투어 탭 UI 레이아웃 개선 (버튼 이동)

'2D 컨투어' 탭에서 '🖼️ 필드 결과 생성' 버튼의 위치를 기존 [ Control ] 그룹에서 '2. 대상 부품 선택' 그룹 하단으로 이동하였습니다. 이는 구조 해석 탭의 '🔍 구조 해석 그래프 생성' 버튼과 일관된 레이아웃을 제공하기 위함입니다.

## 작업 상세

### [MODIFY] [postprocess_ui.py](file:///c:/Users/GOODMAN/WHToolsBox/TVPackageMotionSim/postprocess_ui.py)

- **위치 이동**: `comp_f` (LabelFrame) 내부의 최하단으로 버튼을 배치하였습니다.
- **부모 위젯 수정**: 기존에 정의되지 않은 `control_group`을 참조하던 오류를 수정하고 `comp_f`를 부모로 지정하였습니다.
- **스타일링**: 버튼 너비를 35로 확장하고 하단 가독성을 위해 `pady=5`를 추가하였습니다.

## 검증 계획

- **수동 확인**: 
    1. '2D 컨투어' 탭 클릭 시 '2. 대상 부품 선택' 영역 하단에 버튼이 배치되었는지 확인.
    2. 버튼 클릭 시 컨투어 생성 로직(`_on_show_contour_frame`)이 정상적으로 동작하는지 확인.
