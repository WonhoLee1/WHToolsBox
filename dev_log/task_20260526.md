- [x] 모니터 창 우측 범례(Legend)의 불필요한 빈 공간 제거
    - [x] `whts_monitor.py` 내의 `subplots_adjust` 우측 여백 설정을 `right=0.88` (기존 `0.90`에서 미세 조정)로 수정
    - [x] 범례 상자와 플롯 경계 간의 간격을 넓히기 위해 `bbox_to_anchor` 값을 `1.01`에서 `1.05`로 변경
- [x] 다중 범례를 Figure 레벨의 단일 통합 범례로 변경
    - [x] 서브플롯 개별 범례를 제거하고, `fig.legend()`를 통해 전체 그림(Figure) 우측 중앙에 1개의 통합 범례만 생성
    - [x] `🔍 View` 메뉴에 `📋 Show Legend` (체크 박스 형태) 추가하여 범례 토글 기능 탑재
    - [x] 범례 비활성화 시 플롯이 화면을 꽉 채우도록 여백 자동 조절 기능 추가 (`right=0.96`으로 자동 확장)
- [x] Model 풀다운 메뉴의 "최근 파일" 텍스트 영어화
    - [x] `whts_control_panel.py` 내의 `🕘 최근 파일` 메뉴 명칭을 `🕘 Recent Files`로 변경
- [x] MuJoCo 시뮬레이션 가속화 최적화 시도 및 원복 완료 (물리 솔버 및 60 Hz 렌더링 롤백)
- [x] 시뮬레이션 파이썬 연산 가속화 최적화 (1번 및 2번 방안) 적용 완료
    - [x] **120 Hz 렌더링 스로틀링:** 매 물리 스텝 뷰어 동기화 오버헤드를 줄이기 위해 `1.0/120.0` (8.33ms) 간격으로만 `self.viewer.sync()` 하도록 적용
    - [x] **소성 변형 연산 조기 리턴 (Early Exit):** 인덴트 문제 없는 Surgical 방식으로 `if d.ncon > 0:` 직후에 `np.intersect1d` 교집합 검사를 주입하여, 트래킹 geom에 접촉이 없을 시 즉시 return 하도록 설계
