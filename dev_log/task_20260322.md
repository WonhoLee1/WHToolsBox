# 작업 내역 (2026-03-22)

## 개요
`run_drop_simulation.py` 및 관련 클래스에서 `weld`와 `contact`의 `solref`, `solimp` 파라미터 적용 방식을 개선합니다.

## 작업 목록
- [ ] 기존 `weld` 파라미터 적용 방식 분석
- [ ] `BaseDiscreteBody` 및 하위 클래스의 `material_props` 수정
- [ ] `config` 구조 개선 (weld vs contact 분리 및 부품별 기본값 적용)
- [ ] 지면 접촉용 `solref`, `solimp` 설정 추가 (Cushion 엣지/모서리 특화)
- [ ] 메인 테스트 코드 업데이트 및 검증

## 상세 계획
1. `run_drop_simulation.py`와 `run_discrete_builder.py` 등의 소스 코드 확인
2. `weld` 요소 생성 시 `solref`, `solimp`가 누락된 부분 수정
3. `config` 파일에 `weld_props`와 `contact_props`를 명확히 구분하여 정의
4. 쿠션의 위치(엣지, 모서리)에 따른 차별화된 접촉 파라미터 적용 로직 구현
5. XML 생성 결과 확인 및 시뮬레이션 동작 검토
