wsl --shutdown

$env:DOCKER_HOST="npipe:////./pipe/docker_engine"

# 또는

$env:DOCKER_HOST="npipe:////./pipe/dockerDesktopLinuxEngine"

신규 구현된 내용에 대한 수정 필요 피드백

- 시뮬레이션 완료 후 reset 버튼이 사용할 수 없나?
   시뮬레이션 중에는 되었는데?
   UI에서 처리할까? 다시 애니메이션을 보고 싶은 경우 다시 시뮬레이션 하게 하고 싶다.

- 데이터 저장 및 post-processing
 .PBA 등은 opencell 외에 모든 바디(soft weld연결이 된)에 대해서 계산/저장

- Post-Processing Explorer v4
  . 기구학 분석 (kinematics), 데이터 종류 변위(Pos)를 선택했을 때, 데이터가 저두 y축 값 0으로 나온다.
  . Corner 위치 표현은 F-T-L, R-B-R 과 같이 Front, Rear, Top, Bottom, Left, Right 의 첫자 조합으로 한다.
  . config에 입력된 drop_mode와 drop_direction 조건을 표시하면 도움이 되겠네
  . 배너가 너무 크다. 원본 보다 2배 이상 확대된것 같다. 배너의 정상 크기에 맞추자
  . 모든 figure에서 사용된 폰트가 현재 시스템에 없는 폰트가 있는 것 같다. 일반적으로 Matplotlib에서 잘 사용하는 폰트로 적용하자.
  . matplotlib 관련 옵션을 상단 풀다운 메뉴에 넣자.
    :  plot의 표준 폰트와 크기를 선택할 수 있는 옵션
    :  plot의 테마를 선택하는 옵션
    : backbone을 qt, pyside, tkinter 중 선택할 수 있는 옵션
    : 열려있는 모든 plot window를 일괄로 닫는 옵션
    : 열려있는 모든 plot window를 화면 앞으로 보내는 명령

 .  구조 해석 지표에 대한 상세한 설명을 포함하는 그림이 마우스를 해당 체크 박스에 올리면 보여주는 팝업 도움말
 . 약어로 표한한 지표들은 전체 이름을 표시
 . 구조해석그래프 생성 시 크기는 지금의 2/3 수준
 . figure window 제목에 '구조 해석 지표 - {figure 번호}'와 같이 종류를 포함하는 문자열 표시

 . 구조 해석 지표를 표시할 body들도 선택할 수 있는 위젯 추가. (체크 상자를 포함하는 리스트, 향후 추가할 바디가 있다면 추가하기 용이하도록 )

 . UI tkinter 테마는 breeze-dark로 하고, 풀다운 상단 메뉴에서 변경가능하도록 메뉴를 추가하라.

 . 컨투어 애니메이션이 동작하지 않네.

 . 컨투어의 legend의 범위는 현재 출력된 값들에 대해서 min-max가 정해지게 하자.
 . 컨투어에 PBA가 없네? PBA를 추가하면 이때는 PBA direction의 벡터를 단면 투영하여 점선으로 표현하라.
 . 컨투어의 영역은 config를 참고해서 전면의 가로-세로 크기의 비에 맞게 x-y축이 그려져라. 즉, x,y축을 블록gird로 처리하지 말고, 실제 길이에 매핑하여 구하라. 2차 이상의 보간을 하여 부드럽게 곡선으로  smooth된 필드값이 나오게 하라. grid 넘어와 x-y 좌표는 쿠션/박스 정의에 사용하는 front (전면)이 노말이 +z이고 가로 x, 세로 y가 되도록 설정한다.

  . 시뮬레이션이 진행되면서 matplotlib로 만든 2D 컨투어가 실시간 플레이 되어서 도는 postprocesing 에서 다시 시뮬레이션을 실행시키면서 mujoco에서 보여주면서 contour도 같이 동기화 하여 보여주는 기능을 구현하고 싶었던 깃이다. (옵션 선택 시)

 . 컨투어을 열은 상태에서 시간 컨트롤을 움직이면 이 컨투어가 자동 변경되어야 한다. (애니메이션 시킬때 이것이 자동 진행되는 것이고)

 . UI 다소 진부하다. 세련되게 변경하고 싶다. 최고의 안을 고민해서 적용해봐라.

  기구학 분석, 구조 해석, 2d 컨투어 탭을 왼쪽에 그림 아이콘을 포함하는 리스트 형 메뉴로 하고,

 . 2d 컨투어에서도 바디를 선택하면 layout 으로 출력되면 좋겠네.
  n by m 의 layout은 3열마다 행이 추가되는 기본 구조로 하고 (auto) 사용자 정의된 layout이 있으면 이를 이용. 이를 초과하면 자동 로직. 풀다운 메뉴에 contour layout 메뉴 하위에 행과 열을 선택할 수 있는 콤보 연결. auto도 메뉴에 있게 하고.
